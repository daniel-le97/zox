module strings;

fun shout(value) {
  return value + "!";
}

fun repeat(value, count) {
  var result = "";
  var i = 0;
  while (i < count) {
    result = result + value;
    i = i + 1;
  }
  return result;
}